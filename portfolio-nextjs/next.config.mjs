/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aktifkan jika ingin memuat gambar dari domain eksternal:
  // images: { remotePatterns: [{ protocol: "https", hostname: "**" }] },
};

export default nextConfig;
